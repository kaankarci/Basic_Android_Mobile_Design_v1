<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kk.instagram_clone_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Instagram_Clone_v1">
        <activity android:name=".PhotoUpdated"></activity>
        <activity android:name=".EditPhoto" />
        <activity android:name=".PostUpdate" />
        <activity android:name=".EditPost" />
        <activity android:name=".MyPost" />
        <activity android:name=".Edit_Profile" />
        <activity android:name=".Gallery" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>